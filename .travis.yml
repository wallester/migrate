language: go

go:
  - 1.14.x

install:
  - curl -sfL https://install.goreleaser.com/github.com/golangci/golangci-lint.sh | BINARY=golangci-lint sh -s -- -b $(go env GOPATH)/bin v1.21.0

script:
  - make install
  - make test
  - make lint

env:
  - GO111MODULE=on

install: true
